ENTRENADOR X
  id INT PRIMARY KEY
  valoracionPromedio INT NOT NULL

CONTRATO_CLIENTE_ENTRENADOR
  id INT PRIMARY KEY
  clienteId INT FOREIGN KEY
  entrenadorId INT FOREIGN KEY
  fechaInicioContrato DATE NOT NULL,
  fechaFinContrato DATE,

CLIENTE
  id INT PRIMARY KEY

ECONOMIA_MENSUAL_ENTRENADOR X
  id INT PRIMARY KEY,
  periodo VARCHAR(7) NOT NULL,  -- Formato YYYY-MM
  entrenadorId VARCHAR(10) NOT NULL,
  ingresos DECIMAL(10,2) NOT NULL,
  costes DECIMAL(10,2) NOT NULL,
  clientesActivos INT NOT NULL,
  clientesPotenciales INT NOT NULL,

CLASES
  id
  entrenadorId
  fecha_clase
  capacidad_max
  asistencia_real
  nombre
  descripcion
  dificultad